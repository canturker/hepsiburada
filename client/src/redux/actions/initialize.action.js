export const INITIALIZE = '[Initialize]';

export const INITIALIZE_CATEGORY_DETAIL = `${INITIALIZE} CATEGORY DETAIL`;

export const initializeCategoryDetail = () => ({
  type: INITIALIZE_CATEGORY_DETAIL,
});
